/* 8.In which quarter of 2020, got the maximum total_sold_quantity? The final
output contains these fields sorted by the total_sold_quantity,
Quarter
total_sold_quantit */


select monthname(date),
case
 when month(date)in (9,10,11) then "Q1"
when month(date)in (12,01,02) then "Q2"
when month(date)in (03,04,05) then "Q3"
when month(date)in (06,07,08) then "Q4"
 else "unknown"
 end as quarters,
 sum(sold_quantity) as total_sold_qty from fact_sales_monthly
 where fiscal_year=2020
 group by quarters,monthname(date)
 order by quarters,total_sold_qty asc;
